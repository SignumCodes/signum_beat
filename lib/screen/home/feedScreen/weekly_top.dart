import 'package:flutter/material.dart';

class WeeklyTop extends StatelessWidget {
  const WeeklyTop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
